import React from 'react';

const Header =  () => (
  <header className="mainHeader">
    <div className="container">
      <h1 className="mainHeader__title">Survey results</h1>
    </div>
  </header>
);

export default Header;
